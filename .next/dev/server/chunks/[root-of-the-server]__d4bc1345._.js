module.exports = [
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/@prisma/client [external] (@prisma/client, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("@prisma/client", () => require("@prisma/client"));

module.exports = mod;
}),
"[externals]/pg [external] (pg, esm_import)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

const mod = await __turbopack_context__.y("pg");

__turbopack_context__.n(mod);
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, true);}),
"[externals]/fs [external] (fs, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("fs", () => require("fs"));

module.exports = mod;
}),
"[externals]/path [external] (path, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("path", () => require("path"));

module.exports = mod;
}),
"[project]/src/lib/prisma.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__,
    "prisma",
    ()=>prisma
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f40$prisma$2f$client__$5b$external$5d$__$2840$prisma$2f$client$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/@prisma/client [external] (@prisma/client, cjs)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$prisma$2f$adapter$2d$pg$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@prisma/adapter-pg/dist/index.mjs [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$pg__$5b$external$5d$__$28$pg$2c$__esm_import$29$__ = __turbopack_context__.i("[externals]/pg [external] (pg, esm_import)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/fs [external] (fs, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/path [external] (path, cjs)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$prisma$2f$adapter$2d$pg$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__,
    __TURBOPACK__imported__module__$5b$externals$5d2f$pg__$5b$external$5d$__$28$pg$2c$__esm_import$29$__
]);
[__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$prisma$2f$adapter$2d$pg$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__, __TURBOPACK__imported__module__$5b$externals$5d2f$pg__$5b$external$5d$__$28$pg$2c$__esm_import$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
;
;
;
const connectionString = process.env.DATABASE_URL;
if (!connectionString) {
    throw new Error("DATABASE_URL is required to initialize Prisma");
}
// Ensure a single PrismaClient/Pool instance across hot reloads in dev.
const globalForPrisma = globalThis;
const defaultCaPath = process.env.NODE_EXTRA_CA_CERTS || __TURBOPACK__imported__module__$5b$externals$5d2f$path__$5b$external$5d$__$28$path$2c$__cjs$29$__["default"].join(process.cwd(), "prod-ca-2021.crt");
const caFileExists = ("TURBOPACK compile-time truthy", 1) ? __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].existsSync(defaultCaPath) : "TURBOPACK unreachable";
const caBuffer = caFileExists ? __TURBOPACK__imported__module__$5b$externals$5d2f$fs__$5b$external$5d$__$28$fs$2c$__cjs$29$__["default"].readFileSync(defaultCaPath) : undefined;
const allowInsecureEnv = process.env.DB_SSL_ALLOW_INSECURE === "1" || process.env.DB_SSL_ALLOW_INSECURE === "true" || process.env.NODE_TLS_REJECT_UNAUTHORIZED === "0";
const connectionTimeoutMs = Number.parseInt(process.env.DB_CONNECTION_TIMEOUT_MS ?? "", 10) || 5000;
const buildPool = (rejectUnauthorized)=>{
    const ssl = {
        rejectUnauthorized
    };
    if (caBuffer) {
        ssl.ca = caBuffer;
    }
    return {
        pool: new __TURBOPACK__imported__module__$5b$externals$5d2f$pg__$5b$external$5d$__$28$pg$2c$__esm_import$29$__["Pool"]({
            connectionString,
            ssl,
            connectionTimeoutMillis: connectionTimeoutMs
        }),
        usedInsecure: !rejectUnauthorized
    };
};
const isTlsError = (error)=>{
    if (!error || typeof error !== "object") return false;
    const code = error.code ?? "";
    const message = error.message ?? "";
    const tlsCodes = [
        "SELF_SIGNED_CERT_IN_CHAIN",
        "DEPTH_ZERO_SELF_SIGNED_CERT",
        "UNABLE_TO_VERIFY_LEAF_SIGNATURE",
        "ERR_TLS_CERT_ALTNAME_INVALID",
        "UNABLE_TO_GET_ISSUER_CERT_LOCALLY"
    ];
    return tlsCodes.includes(code) || message.toLowerCase().includes("certificate");
};
const initPool = async ()=>{
    const primary = buildPool(!allowInsecureEnv);
    const warmupResult = await primary.pool.query("SELECT 1").then(()=>"ok").catch((error)=>{
        if (!allowInsecureEnv && isTlsError(error)) {
            return "tls-error";
        }
        console.warn("Database warm-up query failed; continuing with current SSL config.", error);
        return "failed";
    });
    if (warmupResult === "tls-error") {
        await primary.pool.end().catch(()=>{});
        console.warn("Database TLS verification failed; retrying with rejectUnauthorized=false. " + "Provide NODE_EXTRA_CA_CERTS with your trusted root to restore verification.");
        return buildPool(false);
    }
    return primary;
};
const { pool, usedInsecure } = globalForPrisma.pool && typeof globalForPrisma.__dbUsedInsecureSsl === "boolean" ? {
    pool: globalForPrisma.pool,
    usedInsecure: globalForPrisma.__dbUsedInsecureSsl
} : await initPool();
const adapter = globalForPrisma.adapter ?? new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$prisma$2f$adapter$2d$pg$2f$dist$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__["PrismaPg"](pool);
const prisma = globalForPrisma.prisma ?? new __TURBOPACK__imported__module__$5b$externals$5d2f40$prisma$2f$client__$5b$external$5d$__$2840$prisma$2f$client$2c$__cjs$29$__["PrismaClient"]({
    adapter,
    log: ("TURBOPACK compile-time truthy", 1) ? [
        "warn",
        "error"
    ] : "TURBOPACK unreachable"
});
if ("TURBOPACK compile-time truthy", 1) {
    globalForPrisma.pool = pool;
    globalForPrisma.adapter = adapter;
    globalForPrisma.prisma = prisma;
    globalForPrisma.__dbUsedInsecureSsl = usedInsecure;
}
const __TURBOPACK__default__export__ = prisma;
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, true);}),
"[externals]/node:fs/promises [external] (node:fs/promises, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:fs/promises", () => require("node:fs/promises"));

module.exports = mod;
}),
"[externals]/node:path [external] (node:path, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:path", () => require("node:path"));

module.exports = mod;
}),
"[project]/src/lib/csv/ballparks.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "loadBallparks",
    ()=>loadBallparks
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$fs$2f$promises__$5b$external$5d$__$28$node$3a$fs$2f$promises$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/node:fs/promises [external] (node:fs/promises, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$path__$5b$external$5d$__$28$node$3a$path$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/node:path [external] (node:path, cjs)");
;
;
const GAMELOGS_DIR = __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$path__$5b$external$5d$__$28$node$3a$path$2c$__cjs$29$__["default"].join(process.cwd(), "data_sources", "NEW_DATA_SETS", "2020-25 GAMELOGS");
const BALLPARKS_CSV_PATH = __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$path__$5b$external$5d$__$28$node$3a$path$2c$__cjs$29$__["default"].join(GAMELOGS_DIR, "ballparks.csv");
let cache = null;
function splitCsvLine(line) {
    const out = [];
    let cur = "";
    let inQuotes = false;
    for(let i = 0; i < line.length; i += 1){
        const ch = line[i];
        if (ch === '"') {
            if (inQuotes && line[i + 1] === '"') {
                cur += '"';
                i += 1;
                continue;
            }
            inQuotes = !inQuotes;
            continue;
        }
        if (ch === "," && !inQuotes) {
            out.push(cur);
            cur = "";
            continue;
        }
        cur += ch;
    }
    out.push(cur);
    return out;
}
async function loadBallparks() {
    const stat = await __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$fs$2f$promises__$5b$external$5d$__$28$node$3a$fs$2f$promises$2c$__cjs$29$__["default"].stat(BALLPARKS_CSV_PATH);
    if (cache && cache.mtimeMs === stat.mtimeMs) return cache.byParkId;
    const csvText = await __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$fs$2f$promises__$5b$external$5d$__$28$node$3a$fs$2f$promises$2c$__cjs$29$__["default"].readFile(BALLPARKS_CSV_PATH, "utf8");
    const text = csvText.replace(/^\uFEFF/, "").trim();
    const lines = text.split(/\r?\n/).filter(Boolean);
    if (lines.length < 2) throw new Error(`ballparks.csv is empty: ${BALLPARKS_CSV_PATH}`);
    const header = splitCsvLine(lines[0]).map((h)=>h.trim().toUpperCase());
    const idx = new Map(header.map((h, i)=>[
            h,
            i
        ]));
    function cell(cols, key) {
        const i = idx.get(key);
        return i === undefined ? "" : (cols[i] ?? "").trim();
    }
    const byParkId = new Map();
    for (const line of lines.slice(1)){
        const cols = splitCsvLine(line);
        const parkId = cell(cols, "PARKID");
        const name = cell(cols, "NAME");
        if (!parkId || !name) continue;
        if (!byParkId.has(parkId)) byParkId.set(parkId, name);
    }
    cache = {
        mtimeMs: stat.mtimeMs,
        byParkId
    };
    return byParkId;
}
}),
"[project]/src/lib/csv/csv.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "parseCsvText",
    ()=>parseCsvText,
    "readCsvFile",
    ()=>readCsvFile,
    "splitCsvLine",
    ()=>splitCsvLine
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$fs$2f$promises__$5b$external$5d$__$28$node$3a$fs$2f$promises$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/node:fs/promises [external] (node:fs/promises, cjs)");
;
function splitCsvLine(line) {
    const out = [];
    let cur = "";
    let inQuotes = false;
    for(let i = 0; i < line.length; i += 1){
        const ch = line[i];
        if (ch === '"') {
            if (inQuotes && line[i + 1] === '"') {
                cur += '"';
                i += 1;
                continue;
            }
            inQuotes = !inQuotes;
            continue;
        }
        if (ch === "," && !inQuotes) {
            out.push(cur);
            cur = "";
            continue;
        }
        cur += ch;
    }
    out.push(cur);
    return out;
}
function parseCsvText(text) {
    const stripped = text.replace(/^\uFEFF/, "").trim();
    if (!stripped) return {
        header: [],
        rows: []
    };
    const lines = stripped.split(/\r?\n/).filter(Boolean);
    if (lines.length === 0) return {
        header: [],
        rows: []
    };
    const header = splitCsvLine(lines[0]).map((h)=>h.trim());
    const rows = lines.slice(1).map((line)=>splitCsvLine(line).map((c)=>c.trim()));
    return {
        header,
        rows
    };
}
async function readCsvFile(filePath) {
    const stat = await __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$fs$2f$promises__$5b$external$5d$__$28$node$3a$fs$2f$promises$2c$__cjs$29$__["default"].stat(filePath);
    const text = await __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$fs$2f$promises__$5b$external$5d$__$28$node$3a$fs$2f$promises$2c$__cjs$29$__["default"].readFile(filePath, "utf8");
    const { header, rows } = parseCsvText(text);
    return {
        header,
        rows,
        mtimeMs: stat.mtimeMs
    };
}
}),
"[project]/src/lib/csv/dailyTeamStats.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "loadDailyTeamStats",
    ()=>loadDailyTeamStats
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$path__$5b$external$5d$__$28$node$3a$path$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/node:path [external] (node:path, cjs)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$csv$2f$csv$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/csv/csv.ts [app-route] (ecmascript)");
;
;
const DAILY_LOGS_DIR = __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$path__$5b$external$5d$__$28$node$3a$path$2c$__cjs$29$__["default"].join(process.cwd(), "data_sources", "NEW_DATA_SETS", "2020-25 DAILY LOGS");
let cache = null;
function asInt(value) {
    const n = Number.parseInt(value, 10);
    return Number.isFinite(n) ? n : 0;
}
async function loadDailyTeamStats(season) {
    const filePath = __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$path__$5b$external$5d$__$28$node$3a$path$2c$__cjs$29$__["default"].join(DAILY_LOGS_DIR, `${season}DAILY_LOGScsvs`, `${season}teamstats.csv`);
    const { header, rows, mtimeMs } = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$csv$2f$csv$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["readCsvFile"])(filePath);
    if (cache && cache.season === season && cache.mtimeMs === mtimeMs) return cache.rows;
    const idx = new Map(header.map((h, i)=>[
            h,
            i
        ]));
    const cell = (cols, key)=>{
        const i = idx.get(key);
        return i === undefined ? "" : cols[i] ?? "";
    };
    const parsed = [];
    for (const cols of rows){
        const stattype = cell(cols, "stattype").toLowerCase();
        if (stattype && stattype !== "value") continue;
        const gid = cell(cols, "gid");
        const team_id = cell(cols, "team");
        const opp = cell(cols, "opp");
        const vishome = cell(cols, "vishome");
        const site = cell(cols, "site");
        const date = cell(cols, "date");
        if (!gid || !team_id || !date) continue;
        parsed.push({
            gid,
            team_id,
            opp,
            vishome: vishome === "h" || vishome === "v" ? vishome : "",
            site,
            date,
            b_hr: asInt(cell(cols, "b_hr")),
            b_pa: asInt(cell(cols, "b_pa")),
            win: asInt(cell(cols, "win")),
            loss: asInt(cell(cols, "loss")),
            tie: asInt(cell(cols, "tie"))
        });
    }
    // Deterministic ordering.
    parsed.sort((a, b)=>{
        if (a.date !== b.date) return a.date.localeCompare(b.date);
        if (a.gid !== b.gid) return a.gid.localeCompare(b.gid);
        return a.team_id.localeCompare(b.team_id);
    });
    cache = {
        season,
        mtimeMs,
        rows: parsed
    };
    return parsed;
}
}),
"[project]/src/lib/csv/teams.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "loadTeams",
    ()=>loadTeams
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$fs$2f$promises__$5b$external$5d$__$28$node$3a$fs$2f$promises$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/node:fs/promises [external] (node:fs/promises, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$path__$5b$external$5d$__$28$node$3a$path$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/node:path [external] (node:path, cjs)");
;
;
const GAMELOGS_DIR = __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$path__$5b$external$5d$__$28$node$3a$path$2c$__cjs$29$__["default"].join(process.cwd(), "data_sources", "NEW_DATA_SETS", "2020-25 GAMELOGS");
const TEAMS_CSV_PATH = __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$path__$5b$external$5d$__$28$node$3a$path$2c$__cjs$29$__["default"].join(GAMELOGS_DIR, "teams.csv");
let cache = null;
function splitCsvLine(line) {
    const out = [];
    let cur = "";
    let inQuotes = false;
    for(let i = 0; i < line.length; i += 1){
        const ch = line[i];
        if (ch === '"') {
            if (inQuotes && line[i + 1] === '"') {
                cur += '"';
                i += 1;
                continue;
            }
            inQuotes = !inQuotes;
            continue;
        }
        if (ch === "," && !inQuotes) {
            out.push(cur);
            cur = "";
            continue;
        }
        cur += ch;
    }
    out.push(cur);
    return out;
}
function parseIntSafe(value) {
    const v = value.trim();
    if (!v) return null;
    const n = Number.parseInt(v, 10);
    return Number.isFinite(n) ? n : null;
}
async function loadTeams(options) {
    const stat = await __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$fs$2f$promises__$5b$external$5d$__$28$node$3a$fs$2f$promises$2c$__cjs$29$__["default"].stat(TEAMS_CSV_PATH);
    if (cache && cache.mtimeMs === stat.mtimeMs) return cache.teams;
    const csvText = await __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$fs$2f$promises__$5b$external$5d$__$28$node$3a$fs$2f$promises$2c$__cjs$29$__["default"].readFile(TEAMS_CSV_PATH, "utf8");
    const text = csvText.replace(/^\uFEFF/, "").trim();
    if (!text) {
        cache = {
            mtimeMs: stat.mtimeMs,
            teams: []
        };
        return [];
    }
    const lines = text.split(/\r?\n/).filter(Boolean);
    if (lines.length < 2) {
        cache = {
            mtimeMs: stat.mtimeMs,
            teams: []
        };
        return [];
    }
    const header = splitCsvLine(lines[0]).map((h)=>h.trim().toUpperCase());
    const idx = new Map(header.map((h, i)=>[
            h,
            i
        ]));
    function cell(cols, key) {
        const i = idx.get(key);
        return i === undefined ? "" : (cols[i] ?? "").trim();
    }
    const minSeason = options?.minSeason ?? 2020;
    const maxSeason = options?.maxSeason ?? 2025;
    const leagues = options?.leagues?.map((l)=>l.trim().toUpperCase()).filter(Boolean) ?? [
        "AL",
        "NL"
    ];
    const bestByTeam = new Map();
    for (const line of lines.slice(1)){
        const cols = splitCsvLine(line);
        const team_id = cell(cols, "TEAM");
        const league = cell(cols, "LEAGUE").toUpperCase();
        const city = cell(cols, "CITY");
        const nickname = cell(cols, "NICKNAME");
        const first = parseIntSafe(cell(cols, "FIRST")) ?? 0;
        const last = parseIntSafe(cell(cols, "LAST")) ?? 9999;
        if (!team_id || !city || !nickname) continue;
        if (leagues.length && league && !leagues.includes(league)) continue;
        if (last < minSeason || first > maxSeason) continue;
        const name = `${city} ${nickname}`;
        const cand = {
            first,
            last,
            league: league || null,
            name
        };
        const existing = bestByTeam.get(team_id);
        if (!existing) {
            bestByTeam.set(team_id, cand);
            continue;
        }
        // Prefer the most recent era (highest FIRST), then lexicographic name for determinism.
        if (cand.first > existing.first) {
            bestByTeam.set(team_id, cand);
        } else if (cand.first === existing.first && cand.name < existing.name) {
            bestByTeam.set(team_id, cand);
        }
    }
    const teams = Array.from(bestByTeam.entries()).map(([team_id, cand])=>({
            team_id,
            team_name: cand.name,
            league: cand.league
        }));
    teams.sort((a, b)=>{
        if (a.team_name !== b.team_name) return a.team_name.localeCompare(b.team_name);
        return a.team_id.localeCompare(b.team_id);
    });
    cache = {
        mtimeMs: stat.mtimeMs,
        teams
    };
    return teams;
}
}),
"[project]/src/app/api/team-dashboard/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "GET",
    ()=>GET
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/server.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/prisma.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$csv$2f$ballparks$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/csv/ballparks.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$csv$2f$dailyTeamStats$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/csv/dailyTeamStats.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$csv$2f$teams$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/csv/teams.ts [app-route] (ecmascript)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__
]);
[__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
;
;
;
const parseDate = (value)=>{
    if (!value) return undefined;
    const parsed = new Date(value);
    return Number.isNaN(parsed.getTime()) ? null : parsed;
};
const logoPath = (abbrev)=>{
    return abbrev ? `/team-logos/${abbrev.toLowerCase()}.svg` : "/team-logos/default.svg";
};
const logoFromRetrosheetTeam = (teamId)=>{
    const id = teamId.trim().toUpperCase();
    if (id === "NYA") return logoPath("nyy");
    if (id === "BOS") return logoPath("bos");
    return logoPath(null);
};
function yyyymmddToIsoDate(value) {
    const v = value.trim();
    if (!/^\d{8}$/.test(v)) return null;
    return `${v.slice(0, 4)}-${v.slice(4, 6)}-${v.slice(6, 8)}`;
}
function isoDateToTimestamp(value) {
    return `${value}T00:00:00.000Z`;
}
function hash32FNV1a(input) {
    let hash = 0x811c9dc5;
    for(let i = 0; i < input.length; i += 1){
        hash ^= input.charCodeAt(i);
        hash = Math.imul(hash, 0x01000193);
    }
    return hash >>> 0;
}
function safeDiv(numer, denom) {
    if (!Number.isFinite(numer) || !Number.isFinite(denom) || denom <= 0) return null;
    return numer / denom;
}
async function loadTeamDashboardFromCsv(args) {
    const { teamId, season, from, to, splitKey: _splitKey } = args;
    void _splitKey;
    const [rows, teams, parks] = await Promise.all([
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$csv$2f$dailyTeamStats$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["loadDailyTeamStats"])(season),
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$csv$2f$teams$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["loadTeams"])().catch(()=>[]),
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$csv$2f$ballparks$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["loadBallparks"])().catch(()=>new Map())
    ]);
    const teamNameById = new Map(teams.map((t)=>[
            t.team_id,
            t.team_name
        ]));
    const teamLeagueById = new Map(teams.map((t)=>[
            t.team_id,
            t.league
        ]));
    const fromIso = from ? from.toISOString().slice(0, 10) : null;
    const toIso = to ? to.toISOString().slice(0, 10) : null;
    const byGid = new Map();
    for (const row of rows){
        if (row.team_id !== teamId) continue;
        const dateIso = yyyymmddToIsoDate(row.date);
        if (!dateIso) continue;
        if (fromIso && dateIso < fromIso) continue;
        if (toIso && dateIso > toIso) continue;
        const existing = byGid.get(row.gid);
        if (existing) {
            existing.hr += row.b_hr;
            existing.pa += row.b_pa;
            existing.win = existing.win || row.win;
            existing.loss = existing.loss || row.loss;
            existing.tie = existing.tie || row.tie;
            continue;
        }
        byGid.set(row.gid, {
            gid: row.gid,
            date: dateIso,
            opp: row.opp,
            vishome: row.vishome,
            site: row.site,
            hr: row.b_hr,
            pa: row.b_pa,
            win: row.win,
            loss: row.loss,
            tie: row.tie
        });
    }
    const games = Array.from(byGid.values());
    games.sort((a, b)=>{
        if (a.date !== b.date) return a.date.localeCompare(b.date);
        return a.gid.localeCompare(b.gid);
    });
    if (!games.length) return null;
    const gamesPlayed = games.length;
    const totalHr = games.reduce((sum, g)=>sum + g.hr, 0);
    const hrPerGame = safeDiv(totalHr, gamesPlayed) ?? 0;
    const homeGames = games.filter((g)=>g.vishome === "h");
    const awayGames = games.filter((g)=>g.vishome === "v");
    const homeHrPerGame = safeDiv(homeGames.reduce((sum, g)=>sum + g.hr, 0), homeGames.length) ?? 0;
    const awayHrPerGame = safeDiv(awayGames.reduce((sum, g)=>sum + g.hr, 0), awayGames.length) ?? 0;
    const monthlyAgg = games.reduce((acc, g)=>{
        const key = g.date.slice(0, 7);
        acc[key] ??= {
            hr: 0,
            games: 0
        };
        acc[key].hr += g.hr;
        acc[key].games += 1;
        return acc;
    }, {});
    const monthly = Object.entries(monthlyAgg).sort(([a], [b])=>a.localeCompare(b)).map(([month, agg])=>({
            label: month,
            hrPerGame: safeDiv(agg.hr, agg.games) ?? 0
        }));
    const teamName = teamNameById.get(teamId) ?? teamId;
    const league = teamLeagueById.get(teamId) ?? "";
    const keyMetrics = [
        {
            id: "hr_per_game",
            label: "HR/Game",
            value: hrPerGame.toFixed(2)
        },
        {
            id: "games_played",
            label: "Games",
            value: gamesPlayed.toString()
        },
        {
            id: "total_hr",
            label: "Total HR",
            value: totalHr.toString()
        }
    ];
    const hrTimeSeries = games.map((g)=>({
            date: isoDateToTimestamp(g.date),
            hr: g.hr
        }));
    const gamesTable = games.slice().sort((a, b)=>{
        if (a.date !== b.date) return b.date.localeCompare(a.date);
        return b.gid.localeCompare(a.gid);
    }).map((g)=>{
        const result = g.win ? "W" : g.loss ? "L" : g.tie ? "T" : undefined;
        return {
            id: hash32FNV1a(g.gid),
            date: isoDateToTimestamp(g.date),
            opponent: teamNameById.get(g.opp) ?? g.opp ?? "Opponent",
            park: parks.get(g.site) ?? g.site ?? "Park",
            result,
            hr: g.hr
        };
    });
    const response = {
        teamInfo: {
            id: 0,
            name: teamName,
            abbrev: teamId,
            league: league ?? "",
            division: "—",
            logoUrl: logoFromRetrosheetTeam(teamId)
        },
        keyMetrics,
        hrTimeSeries,
        pitcherVulnerability: [],
        upcomingGames: [],
        splits: {
            overview: [
                {
                    label: "Overall HR/G",
                    hrPerGame
                },
                {
                    label: "Home HR/G",
                    hrPerGame: homeHrPerGame
                },
                {
                    label: "Away HR/G",
                    hrPerGame: awayHrPerGame
                }
            ],
            homeAway: [
                {
                    label: "Home HR/G",
                    hrPerGame: homeHrPerGame
                },
                {
                    label: "Away HR/G",
                    hrPerGame: awayHrPerGame
                }
            ],
            lhpRhp: [],
            monthly
        },
        games: gamesTable
    };
    return response;
}
async function GET(req) {
    const { searchParams } = new URL(req.url);
    const teamIdParam = searchParams.get("teamId");
    const teamIdAlt = searchParams.get("team_id");
    const seasonParam = searchParams.get("season");
    const splitParam = searchParams.get("split") ?? "overall";
    const fromParam = searchParams.get("from");
    const toParam = searchParams.get("to");
    const seasonParsed = seasonParam ? Number(seasonParam) : undefined;
    if (seasonParam && Number.isNaN(seasonParsed)) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "season must be a number when provided"
        }, {
            status: 400
        });
    }
    const seasonValue = typeof seasonParsed === "number" ? seasonParsed : 2024;
    const season = seasonParam ? seasonValue : undefined;
    const fromDate = parseDate(fromParam);
    const toDate = parseDate(toParam);
    if (fromParam && fromDate === null) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "from must be a valid date (YYYY-MM-DD)"
        }, {
            status: 400
        });
    }
    if (toParam && toDate === null) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "to must be a valid date (YYYY-MM-DD)"
        }, {
            status: 400
        });
    }
    const mode = (process.env.HOTBAT_BACKEND ?? "auto").toLowerCase();
    const rawTeamId = (teamIdAlt ?? teamIdParam ?? "").trim();
    if (!rawTeamId) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "teamId query param is required"
        }, {
            status: 400
        });
    }
    if (mode === "csv" || teamIdAlt && mode !== "db") {
        const csv = await loadTeamDashboardFromCsv({
            teamId: rawTeamId,
            season: seasonValue,
            from: fromDate === null ? undefined : fromDate,
            to: toDate === null ? undefined : toDate,
            splitKey: splitParam
        });
        if (!csv) return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "Team not found"
        }, {
            status: 404
        });
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json(csv, {
            status: 200,
            headers: {
                "x-hotbat-source": "csv"
            }
        });
    }
    const teamId = Number(rawTeamId);
    if (Number.isNaN(teamId)) {
        if (mode === "auto") {
            const csv = await loadTeamDashboardFromCsv({
                teamId: rawTeamId,
                season: seasonValue,
                from: fromDate === null ? undefined : fromDate,
                to: toDate === null ? undefined : toDate,
                splitKey: splitParam
            });
            if (!csv) return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: "Team not found"
            }, {
                status: 404
            });
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json(csv, {
                status: 200,
                headers: {
                    "x-hotbat-source": "csv"
                }
            });
        }
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "teamId must be a valid integer when HOTBAT_BACKEND=db"
        }, {
            status: 400
        });
    }
    const team = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].team.findUnique({
        where: {
            id: teamId
        },
        select: {
            id: true,
            name: true,
            abbrev: true,
            league: true,
            division: true,
            logoUrl: true
        }
    });
    if (!team) {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "Team not found"
        }, {
            status: 404
        });
    }
    const dateFilter = fromDate || toDate ? {
        date: {
            ...fromDate ? {
                gte: fromDate
            } : {},
            ...toDate ? {
                lte: toDate
            } : {}
        }
    } : {};
    const stats = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].teamGameHrStats.findMany({
        where: {
            teamId,
            ...season ? {
                season
            } : {},
            ...dateFilter
        },
        orderBy: {
            date: "asc"
        },
        include: {
            opponentTeam: true,
            game: {
                include: {
                    park: true
                }
            }
        }
    });
    const summaries = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].teamHrSummary.findMany({
        where: {
            teamId,
            ...season ? {
                season
            } : {}
        },
        orderBy: [
            {
                season: "desc"
            },
            {
                updatedAt: "desc"
            }
        ]
    });
    const summary = summaries.find((s)=>s.splitKey === splitParam) ?? summaries.find((s)=>s.splitKey === "overall");
    const keyMetrics = summary ? [
        {
            id: "hr_per_game",
            label: "HR/Game",
            value: summary.hrPerGame.toFixed(2)
        },
        {
            id: "games_played",
            label: "Games",
            value: summary.gamesPlayed.toString()
        },
        {
            id: "total_hr",
            label: "Total HR",
            value: summary.hr.toString()
        },
        summary.hrVsLeaguePct !== null && summary.hrVsLeaguePct !== undefined ? {
            id: "hr_vs_league_pct",
            label: "HR vs League %",
            value: `${summary.hrVsLeaguePct.toFixed(1)}%`
        } : undefined,
        summary.avgEv !== null && summary.avgEv !== undefined ? {
            id: "avg_ev",
            label: "Avg EV",
            value: summary.avgEv.toFixed(1)
        } : undefined
    ].filter(Boolean) : [];
    const hrTimeSeries = stats.map((s)=>({
            date: s.date.toISOString(),
            hr: s.hr,
            xHr: s.xHr ?? undefined,
            avgEv: s.avgEv ?? undefined,
            barrels: s.barrels ?? undefined
        }));
    const games = stats.map((s)=>{
        const game = s.game;
        const teamScore = game.homeTeamId === s.teamId ? game.homeScore : game.awayScore;
        const opponentScore = game.homeTeamId === s.teamId ? game.awayScore : game.homeScore;
        const result = teamScore !== null && opponentScore !== null && teamScore !== undefined && opponentScore !== undefined ? `${teamScore > opponentScore ? "W" : "L"} ${teamScore}-${opponentScore}` : undefined;
        return {
            id: game.id,
            date: game.date.toISOString(),
            opponent: s.opponentTeam?.name ?? "Unknown",
            park: game.park.name,
            result,
            hr: s.hr,
            xHr: s.xHr ?? undefined,
            hrDiff: s.xHr !== null && s.xHr !== undefined ? Number((s.hr - s.xHr).toFixed(2)) : undefined,
            opposingSp: undefined,
            opposingSpHr9: undefined
        };
    });
    const splits = {
        overview: summaries.filter((s)=>s.splitKey === "overall").map((s)=>({
                label: "Overall HR/G",
                hrPerGame: s.hrPerGame
            })) ?? [],
        homeAway: [],
        lhpRhp: [],
        monthly: []
    };
    const upcoming = await __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$prisma$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].game.findMany({
        where: {
            OR: [
                {
                    homeTeamId: teamId
                },
                {
                    awayTeamId: teamId
                }
            ],
            status: "scheduled",
            ...season ? {
                season
            } : {}
        },
        orderBy: {
            date: "asc"
        },
        include: {
            homeTeam: true,
            awayTeam: true,
            park: {
                include: {
                    parkHrFactors: {
                        where: season ? {
                            season
                        } : {},
                        orderBy: {
                            updatedAt: "desc"
                        },
                        take: 1
                    }
                }
            },
            gamePredictions: {
                where: {
                    teamId
                },
                orderBy: {
                    predictionTimestamp: "desc"
                },
                take: 1
            }
        }
    });
    const upcomingGames = upcoming.map((g)=>{
        const isHome = g.homeTeamId === teamId;
        const opponent = isHome ? g.awayTeam : g.homeTeam;
        const prediction = g.gamePredictions[0];
        const parkFactor = g.park.parkHrFactors[0]?.hrFactor ?? g.park.defaultHrFactor ?? undefined;
        return {
            gameId: g.id,
            date: g.date.toISOString(),
            opponentName: opponent.name,
            opponentAbbrev: opponent.abbrev,
            parkName: g.park.name,
            parkHrFactor: parkFactor,
            predictedHrMean: prediction?.predictedHrMean ?? undefined,
            predictedHrStd: prediction?.predictedHrStd ?? undefined,
            hotbatScore: prediction?.hotbatScore ?? undefined
        };
    });
    const response = {
        teamInfo: {
            id: team.id,
            name: team.name,
            abbrev: team.abbrev,
            league: team.league,
            division: team.division,
            logoUrl: logoPath(team.abbrev)
        },
        keyMetrics,
        hrTimeSeries,
        pitcherVulnerability: [],
        upcomingGames,
        splits,
        games
    };
    return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json(response);
}
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__d4bc1345._.js.map